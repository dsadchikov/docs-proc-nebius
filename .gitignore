__pycache__/
*.pyc
*.pyo
.env
.env.local
.secrets.env
*.pem
*.key
.hypothesis/
.pytest_cache/
.venv/
venv/
.DS_Store
INTERNAL-RUNBOOK.md
internal-docs/
presentation/
CLAUDE.md

# Local-only specs (.kiro): contain internal references — kept out of the public repo
.kiro/

# Local-only Claude Code / kfc spec-workflow tooling (incl. .claude/specs -> .kiro symlink)
.claude/

# Source MIDV-2020 delivery (curated subset lives in app/static/samples/) — raw images/annotations only;
# diagrams and blog screenshots (*.png) in images/ are tracked.
/images/*.jpg
/images/*.json
